<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Autorizacion Server</title>
  <link rel="icon" href="/img/favicon.ico" type="image/x-icon" />
  <!-- styles -->
  <link rel="stylesheet" href="/css/styles.css">
  <!-- <link rel="stylesheet" href="/css/estilos.css">
    <link rel="stylesheet" href="/css/navbar.css"> -->
</head>

<body class="antialiased border-top-wide border-primary d-flex flex-column">
  <div class="page page-center">
    <div class="container-tight py-6">
      <div class="text-center mb-4">
        <a href="."><img src="/img/logoFormularios.png" height="36" alt="" /></a>
      </div>
      <% if(typeof alerts !== 'undefined') { %>
      <% alerts.map(function (alert){ %>
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <%- alert.msg %>
      </div>
      <% }) %>
      <% } %>
      <form class="card card-md" action="/auth/auth" method="POST">
        <div class="card-body">
          <h2 class="card-title text-center mb-4">Iniciar sesión</h2>
          <div class="mb-3">
            <label class="form-label">Usuario</label>
            <input type="text" class="form-control" name="userid" value="<%- datos.userid %>" style="text-transform: lowercase;" autofocus />
          </div>
          <div class="mb-2">
            <label class="form-label">Contraseña</label>
            <input type="password" class="form-control" name="pwdusu" />
          </div>
          <div class="mb-2">
            <span class="form-label">
              <a href="/auth/forgot">He olvidado mi contraseña</a>
            </span>
          </div>
          <div class="form-footer">
            <button type="submit" class="btn btn-primary w-100">
              Identificarme
            </button>
          </div>
        </div>
      </form>
    </div>
  </div>
</body>

</html>