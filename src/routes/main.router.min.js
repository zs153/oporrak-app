/*! oporrak 2023-05-13 */
import express from"express";import authRoutes from"../middleware/auth";import{cleanPage,mainPage}from"../controllers/main.controller";const mainRouter=express.Router();mainRouter.get("/",mainPage),mainRouter.get("/clean",authRoutes,cleanPage);export default mainRouter;