<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <title>Usuarios</title>
  <link rel="icon" href="/img/favicon.ico" type="image/x-icon">
  <!-- styles -->
  <link rel="stylesheet" href="/css/styles.css">
  <script>
    const getCookie = (key) => {
      let value = ''
      document.cookie.split(';').forEach((e) => {
        if (e.includes(key)) {
          value = e.split('=')[1]
        }
      })
      return value
    }
    const setCookie = (name, value, days) => {
      let expires = "";
      if (days) {
        let date = new Date();
        date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
        expires = "; expires=" + date.toUTCString();
      }
      document.cookie = name + "=" + (value || "") + expires + "; path=/";
    }
    const deleteCookie = () => {
      document.cookie = 'filtro=; expires=Thu, 01 Jan 1970 00:00:01 GMT; Path=/;'
    }
  </script>
</head>

<body class="antialiased">
  <div class="wrapper">
    <%- include('../../../../partials/header') %>
    <%- include('../../../../partials/navbar', { opcion: 'CURSO' }) %>
    <div class="page-wrapper">
      <div class="container-xl">
        <div class="page-body">
          <div class="row">
              <div class="col-12">
                <% if(typeof alerts !== 'undefined' ) { %>
                  <% alerts.map(function (alert){ %>
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                      <%- alert.msg %>
                    </div>
                  <% }) %>
                <% } %>
                <div class="card">
                  <div class="card-header">
                    <div>
                      <h3 class="card-title">Usuarios Pendientes de Matricular</h3>
                      <span class="text-muted">
                        <small>
                          Turno: <%- datos.turno.DESTUR %>
                        </small>
                      </span>
                    </div>
                    <div class="col-auto ms-auto">
                      <div class="d-flex">
                        <form action="/admin/cursos/turnos/usuarios/add/<%- datos.curso.IDCURS %>/<%- datos.turno.IDTURN %>" method="GET">
                          <div class="input-icon">
                            <span class="input-icon-addon">
                              <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2"
                                stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                <path stroke="none" d="M0 0h24v24H0z" fill="none"></path>
                                <circle cx="10" cy="10" r="7"></circle>
                                <line x1="21" y1="21" x2="15" y2="15"></line>
                              </svg>
                            </span>
                            <input type="text" class="form-control d-inline-block" id="buscarUserBox" name="part" placeholder="Buscar..."
                              aria-label="Buscar en usuarios">
                          </div>
                        </form>
                      </div>
                    </div>
                  </div>
                  <div class="card-body overflow-auto">
                    <table class="table table-vcenter table-striped sortable table-fixHead" id="tblUsuarios">
                      <thead>
                        <tr class="header">
                          <th class="w-4"></th>
                          <th>Nombre</th>
                          <th class="w-20">Oficina</th>
                          <th class="w-0"></th>
                          <th class="w-0"></th>
                        </tr>
                      </thead>
                      <tbody id="table-body">
                      </tbody>
                    </table>
                    <!-- <div id="pagination-wrapper" class="py-1"></div> -->
                  </div>
                  <div class="card-footer">
                    <form action="#" id="add" method="POST" onsubmit="return addUsuarios();">
                      <div class="col-12 d-flex">
                        <a href="/admin/cursos/turnos/usuarios/<%- datos.curso.IDCURS %>/<%- datos.turno.IDTURN %>" onclick="deleteCookie();" class="btn btn-link">Volver</a>
                        <input type="hidden" name="idcurs" id="idcurs" value="<%- datos.curso.IDCURS %>">
                        <input type="hidden" name="idturn" id="idturn" value="<%- datos.turno.IDTURN %>">
                        <input type="hidden" name="arrusu" id="arrusu" value="">
                        <button type="submit" class="btn btn-link ms-auto" id="btnAdd">Añadir usuarios</button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
          </div>
        </div>
      </div>
    </div>
    <%- include('../../../../partials/footer') %>
  </div>

  <script src="/js/ayuda.min.js"></script>
  <script src="/js/selectUsuariosTurno.js"></script>
  <script>
    const orgList = <%- JSON.stringify(datos.usuarios) %>
    const cursor = <%- JSON.stringify(datos.cursor) %>
    const curso = <%- JSON.stringify(datos.curso) %>
    const turno = <%- JSON.stringify(datos.turno) %>
    const hasNextUsers = <%- datos.hasNextUsers %>
    const hasPrevUsers = <%- datos.hasPrevUsers %>
    const state = {
      'querySet': <%- JSON.stringify(datos.usuarios) %>,
    }

  // events
    const elemBuscar = document.getElementById('buscarUserBox')
    elemBuscar.onchange = (event) => {
      setCookie('filtro', event.target.value, .5)
    }
    elemBuscar.value = getCookie('filtro')

    // inicializar
    const elemAdd = document.getElementById('add')
    elemAdd.setAttribute('action', `/admin/cursos/turnos/usuarios/insert?part=${getCookie('filtro')}`)

    buildTable(state)
  </script>
</body>

</html>
