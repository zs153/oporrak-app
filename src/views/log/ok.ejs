<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Usuario registrado</title>
    <link rel="icon" href="/img/favicon.ico" type="image/x-icon" />
    <!-- CSS files -->
    <link rel="stylesheet" href="/css/estilos.min.css" />
  </head>
  <body class="anti aliased border-top-wide border-primary d-flex flex-column">
    <div class="flex-fill d-flex align-items-center justify-content-center">
      <div class="container-tight">
        <div class="empty">
          <div class="empty-header">Usuario Registrado</div>
          <p class="empty-title">
            Se ha enviado un correo electrónico con su userid y contraseña
          </p>
          <p class="empty-subtitle text-muted">
            La contraseña puede cambiarse en el menu de usuario una vez iniciada
            la sesión
          </p>
          <% if(typeof alerts !='undefined') { %> <% alerts.map(function
          (alert){ %>
          <div
            class="alert alert-warning alert-dismissible fade show"
            role="alert"
          >
            <%- alert.msg %>
          </div>
          <% }) %> <% } %>
          <div class="empty-action">
            <a href="/" class="btn btn-primary">Iniciar sesión</a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
